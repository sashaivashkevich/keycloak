# Keycloak. How to expant jwt token 

## Custom protocol mapper

You can add custom dynamic user attributes to the JWT tokens. Example, you can expan jwt token by querying your custom database.

## install provider